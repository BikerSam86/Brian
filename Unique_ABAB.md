symbol: 🔄🧱

function:
Defines dual-bound symbolic connectors with polarity, used for modeling
recursive interlocking patterns like attitude → behavior → attitude.

structure:
  - NODE_A emits outward (attitude)
  - NODE_B receives and modulates (behavior)
  - BEHAVIOR reflects back to alter attitude

loop:
  A → B → A → B … (φ-constrained recursion)

binding protocol:
  - EMIT(unique keycode from A)
  - B sweeps for matching receptor
  - Upon match, bind as symbolic Lego socket/stud
  - Feedback begins

safety:
  - Keys are unique to prevent crosslinking
  - Reverse path only opens once handshake verified

note:
Modeled after introspective architecture and bidirectional neuroplastic memory wiring.
