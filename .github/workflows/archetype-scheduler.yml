name: Archetype Scheduler 🧬

"on":
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  rotate-archetypes:
    name: Triadic Rotation Rebalance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rotate Archetypes
        run: |
          echo "🔁 Rotating active archetypes: Jester → Engineer → Alchemist"
          echo "🧠 Cycle complete"

